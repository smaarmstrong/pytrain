import functools
from collections.abc import Callable
from typing import ParamSpec, TypeVar

# TODO: declare P = ParamSpec("P") and R = TypeVar("R")


def apply_twice(fn, value):
    raise NotImplementedError


def retry(times):
    raise NotImplementedError
