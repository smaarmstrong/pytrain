def chunks(items, size):
    """Yield lists of up to `size` consecutive elements, lazily."""
    raise NotImplementedError


def averager():
    """Prime with next(); each .send(x) yields the running mean (float)."""
    raise NotImplementedError
