def word_counts(text):
    """Return a mapping of lowercased word -> count."""
    raise NotImplementedError


def top_n(text, n):
    """Return the n most common (word, count) pairs; ties alphabetical."""
    raise NotImplementedError


def group_by_length(words):
    """Return {length: [words of that length, in input order]}."""
    raise NotImplementedError


def tail(iterable, n):
    """Return the last n items of a (possibly one-shot, huge) iterable."""
    raise NotImplementedError
