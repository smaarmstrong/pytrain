from enum import Flag, IntEnum, StrEnum, auto  # noqa: F401  (all of these are useful)

# Define Priority(IntEnum), Color(StrEnum) and Permission(Flag) here —
# see prompt.md for the exact members and values.


def parse_permissions(names):
    """Combine permission names (case-insensitive) into one Permission value."""
    raise NotImplementedError
