"""The string calculator — implement add() guided by the failing tests.

Run the given suite with:  python -m pytest -q
"""


def add(numbers):
    raise NotImplementedError("write just enough code to fix the first failing test")
