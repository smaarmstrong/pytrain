"""Write your parametrized tests for to_roman here.

Run them locally with:  python -m pytest -q
Check the ids with:     python -m pytest --collect-only -q
Boundary cases only:    python -m pytest -m boundary -q
"""
import pytest  # noqa: F401 — you'll need pytest.param / pytest.mark

from roman import to_roman


def test_replace_me():
    # TODO: replace with the parametrized tests described in the prompt.
    assert False, "write your tests"
