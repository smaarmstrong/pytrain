"""Write your float-aware tests for stats here.

Run them locally with:  python -m pytest -q
"""
import pytest  # noqa: F401 — you'll want pytest.approx and pytest.raises

from stats import mean, normalize, variance  # noqa: F401


def test_replace_me():
    # TODO: replace with tests that use pytest.approx for every float result.
    assert False, "write your tests"
