"""Write your tests for slugify here.

Run them locally with:  python -m pytest -q
"""
from slugify import slugify


def test_replace_me():
    # TODO: replace with focused tests covering every rule in the prompt.
    assert False, "write your tests"
