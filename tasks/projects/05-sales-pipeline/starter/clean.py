"""Cleaning layer (suggested split)."""


def clean(df):
    """Return the cleaned DataFrame: product stripped, invalid rows dropped,
    duplicate order_ids dropped (first wins), `revenue` column added.

    See prompt.md for the exact rules and their order.
    """
    raise NotImplementedError
