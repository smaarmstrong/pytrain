"""Guestbook blueprint — expose a module-level Blueprint named `bp`.

Routes (relative to the blueprint): GET /, POST /, GET /<int:entry_id>.
POST accepts form data or JSON; responses are JSON. See prompt.md.
"""
# from flask import Blueprint, current_app, jsonify, request

# bp = Blueprint("entries", __name__)
