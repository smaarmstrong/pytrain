# todo CLI — scaffold

Your entry point is `todo.py`; `storage.py` is a suggested home for the JSON
persistence layer. The grader only ever runs `python todo.py ...` as a
subprocess — how you split the code between the files is up to you.

The full spec (commands, exact output, exit codes, JSON format) is in the
task prompt: see `prompt.md`.
