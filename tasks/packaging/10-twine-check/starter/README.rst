shiny
=====

Polish your strings until they sparkle.

.. bogus-directive::

   This directive does not exist, so this reStructuredText fails to render —
   exactly the kind of thing ``twine check`` catches before you upload.
