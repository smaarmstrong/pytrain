"""greet — tiny greeting helpers (the module you are packaging).

This file is done; the task is to write pyproject.toml next to it.
"""


def hello(name):
    return f"Hello, {name}!"


def shout(name):
    return f"HELLO, {name.upper()}!"
