[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "textkit"
version = "1.0.0"
description = "Tiny text utilities: slugify and squeeze"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []
