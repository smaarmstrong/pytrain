"""quotes — a tiny quote library that ships its data file."""


def get_quote(n):
    """Return quote number n (0-based) from the bundled quotes.txt."""
    raise NotImplementedError  # read quotes.txt via importlib.resources
